let numeroGerado = parseInt(Marth.random() * 100); //parseint serve para armazenar numeros inteiros
console.log(numeroGerado);
let tentativas = [];

numeroGerado.oncharge = ()=> {
    let numeroDigitado = Number (numero.value);
    if (tentativas.length < 10){
        if(tentativas. legth >0){
            for (let i = 0; i < tentativas.length; i++){
                if(tentativas[i] == numeroDigitado){
                    
                }
            }
        }
    }
}


